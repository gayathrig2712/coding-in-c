#include<stdio.h>
int main(void)
{
   int arr[]={12,23,34,45,56,67,78},i;
    int len=sizeof(arr)/sizeof(int);
    int cnt=0;
    int n=arr[0];
    for(i=1;i<len;i++)
    {
      if(arr[i]==n)
      cnt++;
    }
    if(cnt==1)
    printf("DUPLICATE");
    if(cnt==0)
    printf("UNIQUE");
}
