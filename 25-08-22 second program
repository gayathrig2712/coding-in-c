#include<stdio.h>
int main(void)
{
    char str[100];
    int i;
    printf("enter the string: ");
    scanf("%s",str);
    for(i=0;str[i]!='\0';i++)
    {
        if(i%2!=0)
        str[i]=str[i]-32;
    }
    printf("the updated string is %s",str);
